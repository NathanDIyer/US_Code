# USC Search Tools

Utilities and scripts for building and searching a U.S. Code (USC) corpus.

## What’s here
- Index/build scripts for USC data
- Search/database utilities
- App and integration helpers

## Quick start
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Run the scripts you need from the repo root.
